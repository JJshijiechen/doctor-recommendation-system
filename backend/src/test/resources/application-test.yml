spring:
  datasource:
    url: jdbc:h2:mem:doctor_recommendation;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  data:
    redis:
      host: localhost
      port: 6379

app:
  security:
    enabled: false
  openai:
    api-key:
  google:
    places-api-key:
  serpapi:
    api-key:
